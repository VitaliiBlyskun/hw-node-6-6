const addContactSchema = require('./addContactSchema');
const updateFavoriteContactSchema = require('./updateFavoriteContactSchema');

module.exports = {
    addContactSchema,
    updateFavoriteContactSchema
}